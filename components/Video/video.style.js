import { Dimensions, StyleSheet } from "react-native";

export default StyleSheet.create({
  video_container: {
    flex: 0.88,
    backgroundColor: "#2a2b40",
    padding: 5,
  },
  video: { backgroundColor: "#2a2b40" },
});
